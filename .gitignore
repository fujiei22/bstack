everything-claude-code
superpowers
gstack

# Claude Code per-cwd 權限白名單（per-machine，不入 repo）
**/.claude/settings.local.json

# Repo root 的 .claude/（EnterWorktree 容器與 per-cwd meta，不入 repo）
/.claude/

# Playwright MCP server 的 local artifact（snapshot / console log）
.playwright-mcp/

# frontend-e2e-runner verify 留下的截圖與報告（agent 的 cwd artifact，不入 repo）
scripts/test-reports/
